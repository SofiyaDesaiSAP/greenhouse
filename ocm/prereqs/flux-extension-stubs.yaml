# Stub CRDs for the Flux Controller Provider framework (source.extensions.fluxcd.io).
#
# Greenhouse v0.16.1+ controller-manager watches ArtifactGenerator from this API group.
# The full Flux extension framework is not shipped with `flux install`.
# This stub unblocks the controller — the ArtifactGenerator feature remains inactive
# until the real framework is installed.
#
# Applied by: make -f Makefile.ocm install-flux-extension-stubs
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: artifactgenerators.source.extensions.fluxcd.io
spec:
  group: source.extensions.fluxcd.io
  names:
    kind: ArtifactGenerator
    listKind: ArtifactGeneratorList
    plural: artifactgenerators
    singular: artifactgenerator
  scope: Namespaced
  versions:
  - name: v1beta1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        x-kubernetes-preserve-unknown-fields: true
